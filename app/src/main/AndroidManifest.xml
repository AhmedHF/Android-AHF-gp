<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ahmedhassan.smartcollege">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo1"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Announcements"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login"> <!-- Login -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentLogin" /> <!-- StudentLogin -->
        <activity android:name=".Semester" />
        <activity
            android:name=".Profile"
            android:configChanges="orientation|keyboardHidden|screenSize">
            />
        </activity>
        <activity android:name=".CourseDetails" />
        <activity android:name=".UpadateProfileActivity" />
        <activity android:name=".ContactsActivity" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".Announcement_Details" />
        <activity android:name=".AddCourses" />
        <activity android:name=".ViewCourses" />
        <activity android:name=".ViewSemesters" />
        <activity android:name=".ViewAllCourses"></activity>
    </application>

</manifest>